export const user = (state) => state.user;
export const pagination = (state) => state.pagination;
export const currPageIndex = (state) => state.currPageIndex;
export const status = (state) => state.status;
